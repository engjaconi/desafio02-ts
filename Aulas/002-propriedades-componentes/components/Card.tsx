/* interface Icard {
    id: number;
    paragraph: string,
    details:string
}

export const Card = ({id, paragraph, details} : Icard) => {
    return (
        <div>
            <h2>Card {id} </h2>
            <p>{ paragraph }</p>
            <p>{ details }</p>
        </div>
    )
} */